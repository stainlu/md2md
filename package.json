{
  "name": "md2md",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first protocol for mapping .md domains into synchronized local Markdown files for AI agents.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-content.mjs",
    "kv:bulk": "node scripts/generate-kv-bulk.mjs",
    "worker:check": "npm --prefix worker run check",
    "check": "npm run validate && npm run worker:check"
  }
}
