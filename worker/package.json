{
  "name": "md-domains-worker",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir ../.wrangler-dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "wrangler": "^4.14.0"
  }
}
